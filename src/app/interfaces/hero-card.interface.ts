export interface HeroCard {
  title: string;
  subtitle: string;
  description: string;
  button: string;
  demo: string;
}
export interface UserCompany {
  name: string;
  title: string;
  description: string;
  imageSrc: string;
  reverse: boolean;
}
